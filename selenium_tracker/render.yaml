services:
  - type: web
    name: selenium-tracker-api
    env: docker
    plan: free
    autoDeploy: true
    healthCheckPath: /healthz
    envVars:
      - key: SELENIUM_TRACKER_CORS_ORIGINS
        value: "*"
      - key: BRAVE_BINARY_PATH
        value: /usr/bin/brave-browser
