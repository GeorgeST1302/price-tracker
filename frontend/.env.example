# Vite env vars are public and shipped to the browser.
# Do not place Telegram or any private keys here.
VITE_API_BASE_URL=http://localhost:8787
# Set this to a public Python API host when using the Selenium tracker from Cloudflare Pages.
# VITE_SELENIUM_TRACKER_API_BASE_URL=https://your-public-selenium-api.example.com
